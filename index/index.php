<?php
$a=1;
public class Test{
	$b=2;
}
// $a=1;
// $b='abc';
// $c='1abc';
// var_dump($a==$b);//bool(false)   $a与$b相比较  1个是整数，一个是字符串，等式不成立返回false
// echo '<br>';
// var_dump($a==$c);//bool(true)	 
// // ==只是对值得比较（将两边值转化为同类型再比较），
// // 对于==的比较，若有一方为数字，另一方为字符串或空或null，均会先将非数字一方转化为0，再做比较。

// echo '<br>';
// var_dump($a===$c);//bool(false) 而===则是对值和类型的比较。 


// //==只是对值得比较（将两边值转化为同类型再比较），而===则是对值和类型的比较。 
// //对于==的比较，若有一方为数字，另一方为字符串或空或null，均会先将非数字一方转化为0，再做比较。
	
// ?>